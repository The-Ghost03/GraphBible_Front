version: '3.8'

services:
  frontend:
    build: .
    container_name: biblegraph_frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
